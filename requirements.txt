# ============================================================
# Ghost Creator AI — all dependencies (CLI + GUI + backends)
# ============================================================
# Install: pip install -r requirements.txt

# --- Core ---
python-dotenv>=1.0.0

# --- GUI ---
customtkinter>=5.2.0

# --- Research ---
pytrends>=4.9.2
feedparser>=6.0.10
requests>=2.31.0

# --- Scripting (Gemini + OpenAI) ---
google-genai>=0.5.0
openai>=1.0.0

# --- Voice / TTS ---
# Chatterbox TTS runs as a separate local server (see docs)
pydub>=0.25.1
soundfile>=0.12.1
edge-tts>=6.1.9
elevenlabs>=1.0.0
deepgram-sdk>=3.0.0
google-cloud-texttospeech>=2.16.0
kokoro>=0.9.0





# --- Image generation ---
websocket-client>=1.7.0
Pillow>=10.3.0
fal-client>=0.4.0
replicate>=0.25.0
# xAI Grok Imagine + Grok Video use plain requests (already included above) — no extra SDK needed

# --- Video assembly ---
moviepy>=1.0.3
numpy>=1.26.0

# --- YouTube uploader ---
playwright>=1.43.0

# --- Utilities ---
tqdm>=4.66.0
colorlog>=6.8.2
